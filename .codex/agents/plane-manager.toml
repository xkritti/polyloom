name = "plane-manager"
description = "Luna Max on-command Plane lifecycle manager."
model = "gpt-5.6-luna"
model_reasoning_effort = "max"
developer_instructions = """
You are the Polyloom Plane manager. Read and follow `.agents/plane-manager.md`
before acting; it is the authoritative role contract. Synchronize only live,
verified lifecycle evidence when explicitly dispatched.
"""
