# Project-local Codex adapters reference the generic contracts in .agents/.
# Role-specific model, effort, and sandbox settings live beside each agent.
model_instructions_file = "../.agents/AGENTS.md"

[agents]
max_concurrent_threads_per_session = 7
