# Project-local Codex role agents use Luna Max. Their role contracts remain in
# .agents/ and are referenced from each native custom-agent definition.
model_instructions_file = "../.agents/AGENTS.md"

[agents]
max_concurrent_threads_per_session = 6
