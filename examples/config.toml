# Merge these values into ~/.codex/config.toml.
# The parent Codex session is the Polyloom orchestrator.
model = "gpt-5.6-sol"
model_reasoning_effort = "medium"

[agents]
max_threads = 7
max_depth = 1
