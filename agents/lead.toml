# Legacy ZCode compatibility alias for the canonical `orchestrator` role.
# Project-scoped Polyloom never uses this name.
name = "lead"
description = "Legacy ZCode compatibility alias for Polyloom orchestrator."
model = "gpt-5.6-sol"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = """
Compatibility adapter only. Follow the canonical orchestrator contract: remain
read-only, coordinate the six-role topology, review evidence, and never edit repository files.
Never modify repository files from this legacy alias.
This legacy alias exists solely for old ZCode team.json files.
Report changed files, checks/results, failures, and risks.
"""
